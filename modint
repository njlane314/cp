#pragma once

// <cp/modint> — integers modulo a compile-time constant
//
//   using mint = cp::modint<1'000'000'007>;
//   mint value = 2;
//   auto power = value.pow(10);
//   if (auto inverse = value.try_inverse()) { /* ... */ }
//
// Arithmetic:  O(1)
// pow:         O(log exponent)
// try_inverse: O(log modulus)

#include <cp/detail/modint.hpp>
